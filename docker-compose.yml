services:
  gestaopedidosapi:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "55705:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    restart: unless-stopped
